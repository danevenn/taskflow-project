{
  "name": "taskflow-project",
  "version": "1.0.0",
  "description": "Esta página web es un ejemplo de una página web con un listado de tareas.",
  "main": "app.js",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./client/input.css -o ./client/style.css",
    "watch:css": "npx @tailwindcss/cli -i ./client/input.css -o ./client/style.css --watch",
    "server": "nodemon server/src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "@tailwindcss/cli": "^4.2.1",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
