{
  "name": "taskflow-project",
  "version": "1.0.0",
  "description": "Esta página web es un ejemplo de una página web con un listado de tareas.",
  "main": "app.js",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./input.css -o ./style.css",
    "watch:css": "npx @tailwindcss/cli -i ./input.css -o ./style.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "tailwindcss": "^4.2.1"
  }
}
